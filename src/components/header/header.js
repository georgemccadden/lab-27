import React from 'react';

class Header extends React.Component {
  render() {
    return (
      <header>
        <h1>Trying Out Enzyme</h1>
        <h4>A counter application that needs testing!</h4>
      </header>
    );
  }
}

export default Header;
